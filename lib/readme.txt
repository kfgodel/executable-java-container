This folder stores the library used to communicate from your app and JVM to the executable container.
These library files are used as native extensions to the VM to communicate with the container process, and allows you
 to restart you own VM, for instance.